# GPU tier dependencies (Windows 8 GB+ NVIDIA GPU / CUDA 12.4 server)
# Inference engine: llama.cpp (GGUF Q8_0), the only inference engine used
# Install order: see README 'Installing the GPU tier (Windows)' (torch is a CUDA-DLL dependency only; add torch/lib to PATH)

# llama-cpp-python 0.3.34 (cu124 GPU wheel, download from a GitHub mirror; see README 'Installing the GPU tier (Windows)')
# PyPI has no Windows GPU wheel for this version
llama-cpp-python==0.3.34

# torch: provides the CUDA runtime DLLs only (cudart64_12/cublas64_12), not an inference dependency
torch==2.8.0

# Data and plotting
datasets==5.0.1
huggingface_hub==0.36.2
numpy==2.5.1
matplotlib==3.11.1
ipywidgets==8.1.9

# Answer normalization (used by scripts/evaluate.py)
sympy==1.14.0

# Model download (ModelScope mirror)
modelscope==1.39.1
