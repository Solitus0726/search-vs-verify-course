# GPU 档依赖清单（Windows 本地 8GB 显卡 / CUDA 12.4 服务器）
# 推理引擎：llama.cpp（GGUF Q8_0），本项目唯一推理引擎
# 安装顺序见 README「安装」节（torch 仅作 CUDA DLL 依赖，需将 torch/lib 加入 PATH）

# llama-cpp-python 0.3.34（cu124 GPU wheel，从 GitHub 加速镜像下载，
# 见 README「安装」节；PyPI/国内镜像无 Windows GPU wheel）
llama-cpp-python==0.3.34

# torch：仅提供 CUDA 运行时 DLL（cudart64_12/cublas64_12），非推理依赖
torch==2.8.0

# 数据与绘图
datasets==5.0.1
huggingface_hub==0.36.2
numpy==2.5.1
matplotlib==3.11.1

# 答案判定规范化（scripts/evaluate.py 依赖）
sympy==1.14.0

# 模型下载（ModelScope 镜像）
modelscope==1.39.1
