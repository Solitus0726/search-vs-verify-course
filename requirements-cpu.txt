# CPU tier dependencies (no-GPU students: use the cached layer, or run small models on CPU)
# Inference engine: llama.cpp CPU wheel (the default PyPI build; no torch needed)

llama-cpp-python==0.3.34

# Data and plotting
datasets==5.0.1
huggingface_hub==0.36.2
numpy==2.5.1
matplotlib==3.11.1
# ipywidgets: prevents tqdm progress-bar warnings in Jupyter
ipywidgets==8.1.9

# Model download (ModelScope mirror)
modelscope==1.39.1
